{
  "type": "module",
  "scripts": {
    "test": "node --test tests/dates.test.mjs tests/functions-validate.test.mjs tests/persistence.test.mjs tests/system.test.mjs tests/sync-model.test.mjs",
    "test:emulator": "set XDG_CONFIG_HOME=.firebase-config&& set FIREBASE_CLI_DISABLE_UPDATE_CHECK=true&& firebase emulators:exec --only firestore \"node --test tests/sync-emulator.test.mjs\""
  },
  "dependencies": {
    "puppeteer": "^25.1.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.31.0"
  }
}
